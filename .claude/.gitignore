# Local/personal files (not shared with team)
claude.local.md
settings.local.json

# MCP files with credentials — add server-specific files here if needed
# .mcp.json is committed since current servers require no credentials

# Hook logs
hooks/*.log

# OS files
.DS_Store
Thumbs.db
